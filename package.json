{
  "name": "matchoracle-android",
  "version": "2.0.0",
  "description": "MatchOracle Android wrapper built with Capacitor",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "cap:build:debug": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "cap:run": "npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "@capacitor/splash-screen": "^5.0.7",
    "@capacitor/status-bar": "^5.0.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
